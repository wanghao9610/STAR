# Claude Code is the tree the others are generated from, so it has no
# substitutions of its own: its vocabulary is the one the rules below rewrite.
# The file exists so .claude is handled like any other tree when deciding which
# of its files are links into .agents/skills.
