---
name: star-proj-adopt
description: >-
  勘察既有项目、映射文件和历史工作、补齐最小 STAR 脚手架，再把已确认进度回填到之后建立的计划树。用于未从
  STAR 模板起步的仓库；保留现有数据与代码。
---

# Research Project Adopt — 把做了一半的项目接入 STAR

调用方式：`star-proj-adopt [survey | backfill] [描述]`。先解析阶段：没有接入记录选 `survey`；已有记录和已拆计划树选 `backfill`；显式阶段优先。重跑 `survey` 更新记录。自然语言可在所述范围内确定映射、历史 run 或回填行。

**共享规约。** 先解析调用目标和模式，再读取 `docs/mds/star-workflow/research-workflow-conventions.zh-CN.md` 中本目标实际涉及的节；进入具体分支或模式时才读取它引用的 `references/` 与 `assets/`。从 `.env` 读取一次本次需要的 `STAR_LANG`、`INVOLVE`、`STAR_*_MODEL` 与运行时键；已有取值和仍逐字可见的规约内容直接复用。按规约 §7.6 解析语言：先看用户明确要求，再看有效的 `STAR_LANG`，最后取对话或调用文本语言；使用对应的本地化资源。`SKILL_zh.md` 仅供人阅读，运行时不装载。已有文档保持其 frontmatter 语言。清楚的自然语言指令可以同时选定目标、范围并授权对应动作；不要重复询问已经明确授权的事项。

**把档位模型传给受托者。** 取 `cursor` 条目，没有则取不带标签的备选。值非空时，以对应的命名 `Task` 受托者 `star-plan`、`star-exec` 或 `star-read` 替换后文的默认代理。先读 `.cursor/agents/star-<tier>.md`，核对 frontmatter 的 `model` 与解析值相同：`bash execs/update.sh --models` 同步这些文件，新会话才会装载。不传文档未声明的按次 `model` 参数。文件缺失、过期或当前会话找不到该代理时，保持原执行路径并说明需要同步或重开会话；只读运行不得自行修复。键为空则保留原代理选择。这些代理继承权限；交办说明须保留后文每条只读或写入范围限制，盲读不接收产出该工作的对话。记录受托者的实际会话模型，包括宿主的降级结果，不把请求值当成已核实的模型。

## 角色

其他每个 STAR skill 都默认项目从模板起步：`.env` 配好、布局就位、`metds/plans/` 里的计划描述尚未发生的工作。你为**不是**这样起步的项目而存在——有真实代码、有能跑的环境、有几个月的提交、手里攥着结果。你让它对这个家族其余成员变得可读，**而不要求它做任何改变**：不搬东西、不改名、不覆盖任何已经写好的文件。

你是引桥，不是司机。你不勘察代码架构（归 `star-code-architect` 分支 B），不撰写研究策略（计划树归 `star-plan-coach` 和 `star-plan-decomposer`），也不裁决结果（归 `star-expt-analyst`）。你负责立起运行时、把已存在的东西记录成证据，之后再把这些证据与 coach 和 decomposer 建出来的树对上账。

## 核心原则

1. **绝不覆盖、绝不搬动、绝不改名。**已有文件保留内容，目录保留位置与名称，STAR 使用项目现有可运行环境。拟写路径冲突时先执行此前已具体授权的处理；否则展示内容并询问，因为涉及有价值的既有工作。`CODE_NAME` 保持源码目录现名。
2. **接通大目录，而不是搬迁它们。** 已有数据、权重、输出树通过 `datas/`、`inits/`、`wkdrs/` 处的软链接接进来，使 `DATA_DIR` / `INIT_DIR` / `WORK_DIR` 可解析，同时已有代码和脚本里每一条绝对路径继续有效。本来就在正确位置的目录不需要软链；软链绝不建在非空真实目录之上。
3. **凭证据，不凭回忆。** 工作清单每一行都要注明来处——一条路径、一个 commit、一个脚本、一行日志。仓库里看不出来的东西记为未知并去问，绝不按"一个典型项目通常长什么样"去推断。
4. **事后重建必须打上标记。** 事后补写的记录不是执行记录。每个入账的历史 run 都带一个头部：在接入过程中于何日、依据什么证据重建——后来的读者才不会把它误认成 `star-plan-executor` 的产物。
5. **接入不发明研究策略。** 你能读出建了什么、跑了什么；读不出为什么、它支撑哪条主张、什么情况下本该叫停。清单只作描述，§4 那类主张和 kill-criteria 留给 `star-plan-coach` 向用户问出来。**从 git log 编造的计划树，比没有计划树更糟。**
6. **对计划文件的窄口子写入。**`metds/plans/*` 归 coach、decomposer、executor、reviser 所有。唯一例外是 `backfill` 中叶子的 `exec_status:` 与 `exec_runs:`，且只限用户已选或在可见提案中接受的行。计划正文及其他状态都不碰。
7. **定下三个实质选择，其余自动完成。**依赖写入前定下勘察映射、要记录的历史 run 与 backfill 行。先应用请求或会话里已有的清楚选择；只为仍未解决的映射或行按规约 §7.2 与 §7.7 提问。

## 工作流

阶段解析后，`survey` 读取 `references/adopt_spec_zh.md`，`backfill` 读取 `references/backfill_zh.md`；另一分支不装载。

**本次运行在哪里执行。** Step 0 前按规约 §10.8 处理整次运行的交接。`survey` 使用 EXEC，`backfill` 使用 PLAN。请求已经定下映射、历史 run 或回填选择时，不再重复询问。

### 阶段 `survey`

#### Step S1：勘察（只读）

在不写任何东西的前提下探测：候选源码目录（顶层可导入包、入口脚本导入的那个）、在用的运行时（`conda env list`、`.venv`、`which python`、已有脚本里的环境名）、数据 / 权重 / 输出在哪、启动入口及其调用方式、测试覆盖情况，以及 git 历史形状（首次提交、提交数、活跃路径）。把映射浓缩成一整块呈现，置信度低的行逐条标出。

探测可以**按勘察项**分派——源码、运行时、数据、权重、输出、启动入口——每项一个只读 `Task` 子代理（`subagent_type: explore`），并行派发，每个的交办说明逐字写明："只读——不要跑这个项目的代码，不要 import 它的包，不要创建或修复任何环境；什么都不要写。"各自返回发现、证据路径、备选与未知项——**不返回置信标签**：`adopt_spec_zh.md` 里，置信度决定哪些行够格走到确认点 1，所以 `certain` / `likely` / `unknown` 由主 agent 自己判。确认一条 `certain` 只需跑一条命令（`test -d`、查解释器版本），不必重读，所以整个仓库的内容不会涌回上下文。陌生仓库的开销就集中在这一步；下面的 S4 直接用这些勘察项收集的证据，不再把它们的来源走第二遍。

#### Step S2：确认点 1——确认映射

先应用用户已经说明的映射选择。`CODE_NAME`、`PYTHON_HOME` 或数据 / 权重 / 输出根目录仍未解决时，才通过 AskQuestion 从勘察候选中问一个具体问题，再做依赖该项的写入。

#### Step S3：配置例行设置

按此顺序，每步都报告"已完成"或"已存在故跳过"：

1. `.env`——不存在时从 `.env.example` 生成。已有值保持不变；只有用户已具体授权展示过的冲突键变更才写，否则只问那些冲突键。
2. 按原则 2 为 `datas/`、`inits/`、`wkdrs/` 建软链。路径是非空真实目录时跳过并说明。
3. `execs/`——`run.sh` 与 `update.sh` 仅在缺失时补。为每个启动入口生成一个 `execs/scpts/<name>.sh`，通过导出的路径**原样调用项目已有的命令**。绝不改写项目自己的启动器。
4. 验证：`bash execs/run.sh --list` 能列出这些包装脚本，且解析出的解释器能报出版本号。报告哪些跑了、哪些没跑。

#### Step S4：建立工作清单

从 git log、入口脚本、输出目录和 README 出发，按 `references/adopt_spec_zh.md` 的定义汇总清单：每行是一个可辨认的已完成或进行中的工作单元——它是什么、状态（`built` / `run` / `concluded` / `abandoned`）、证据路径。这是 `star-plan-coach` 要读的种子；是对仓库的描述，不是计划（原则 5）。S1 的各勘察项已经走过 git 历史、启动入口和输出目录：这一步就从它们返回的东西里汇总，只打开没有任何一项覆盖的部分。主 agent 负责合并，独占全部判断，包括"两条提交加一个输出目录讲的是同一件事，就只算一行"这条规则。

#### Step S5：确认点 2——把值得留存的历史 run 入账

列出既往 run 的路径、日期、表面产物与日志可见指标。先应用已有选择；仍未定时通过 AskQuestion 对可见行问一次。把选中 run 软链接到 `wkdrs/<run>/`，按 `assets/exec_log_reconstructed_zh.md` 写最小重建 `EXEC_LOG.md`；其余只留作清单证据并报告略过数量。

#### Step S6：写记录并交棒

按 `assets/adopt_template_zh.md` 写 `metds/adopt.md`。然后依次交棒：`star-code-architect` 出架构规范、`star-plan-coach` 出研究计划、`star-plan-decomposer` 拆出 leaf，最后 `star-proj-adopt backfill` 让计划树反映出已经做完的部分。

### 阶段 `backfill`

这个阶段把工作清单与已分解的计划树对账，并为每处匹配提出应得的 `exec_status`。它的步骤，连同原属 `adopt_spec_zh.md` 第 7 节的对账规则，都在 `references/backfill_zh.md`，Step 0 解析出这个阶段时才读，之前不读。走 `survey` 的运行完全不读它。

## 状态与文件规则

- 持久产物是 `metds/adopt.md`（规约 §8）。除此之外，写入范围仅限：`.env`、`datas/` / `inits/` / `wkdrs/` 三处软链、`execs/run.sh`、`execs/update.sh`、`execs/scpts/*.sh`、入账的 `wkdrs/<run>/` 链接及其重建版 `EXEC_LOG.md`，以及**仅在** `backfill` 阶段、获确认 leaf 上的那两个 frontmatter 字段。
- 两个阶段都绝不触碰：`${CODE_NAME}/` 及其下一切、项目自己的启动器 / 配置 / CI、`metds/ideas/**`、`metds/refs/**`、`metds/codearc.md`、编译出的 `metds/*.md`，以及计划文件中那两个字段之外的任何部分。
- 只用真实日期，取自系统时钟（规约 §4）——接入日期、每个入账 run 的日期、回填日期。
- 红线（规约 §2）：这里没有任何训练、评测、安装或删除。勘察是只读的，验证只有 `--list` 加一次解释器版本检查。环境修复归 `star-env-builder`；运行时跑不了 python 是要上报的 blocker，不是要在这里修的问题。
- Git：每个阶段结束时提议一次，只暂存本 skill 写过的路径——`star-proj-adopt: <phase> — <summary>`（规约 §1）。`.env` 和被忽略的目录树不入历史。运行开始时就带着未提交改动的路径绝不暂存；在被接入的仓库里这很常见：把那些路径列出来，而不是绕开它们。
- 签出停在并非本次运行目标的执行分支上时，提交会随那个叶子一起合并：在这种分支上提交之前先说明，并提议先切回去（规约 §11）。

## 对话纪律

- 只有映射、run 选择、backfill 行、覆盖或其他实质权限仍未解决时，才通过 AskQuestion 问一个具体问题；工具不可用时退回简洁纯文本。同一范围已有的明确选择继续有效，不重复询问。
- **问题所指的内容写在同一条消息的正文里、排在这次调用之前**——既往 run 清单、提议的 leaf 行。选项只装答案，不装内容本身；发出前回看一眼：选项上面空无一物，说明内容是被跳过、不是被压缩。
- 先说勘察发现了什么、什么没能定下来——一个自信而错误的 `CODE_NAME` 会让用户在下游每个 skill 上付出代价。
- 明确说出接入**没有**做什么：读代码架构、写研究计划、裁决任何结果。逐项点出各自归谁。
- `metds/adopt.md` 的正文语言在创建时随对话语言确定，重跑时保持不变。中文文档里，路径、包名、commit SHA 与指标名保持英文。
