---
run: <run>
source_plan: (none — 该 run 早于计划树)
reconstructed: <YYYY-MM-DD>
reconstructed_by: star-proj-adopt
updated: <YYYY-MM-DD>
status: done
---

# Execution Log — <run>（重建）

> **这不是一份执行记录。** 它是在项目接入过程中，于 <YYYY-MM-DD> 由 `/star-proj-adopt` 依据磁盘上已有
> 的产物重建出来的。没有任何人观察过这些步骤的运行，当时也没有记录任何步级进度。下面每一行都应当读作
> 事后找到的证据，而不是当时随着发生被记录下来的东西。

## 这个 run 是什么

<!-- 一到两行，取自选中该 run 的那条清单行。它看起来产出了什么，用仓库自己的术语。 -->

## 命令

<!-- 仅当能从脚本、存档配置或日志头部逐字复原时才写调用命令。否则原样写：「无法从磁盘上的产物复原。」
     绝不重建一条看起来合理的命令——一条看起来合理的命令，是一条看起来像对的错命令。 -->

## 现存产物

<!-- run 目录里现在实际有什么：checkpoint、指标文件、日志、图。路径相对本文件。有助于理解时给出大小
     或数量。 -->

## 找到的指标

<!-- 从日志、结果文件或 README 里逐字引用的数字，各自注明出处文件。绝不计算，绝不四舍五入，绝不换算。
     没有可见指标时留空。 -->

| 指标 | 值（逐字） | 出处 |
|---|---|---|

## 未记录的部分

<!-- 刻意保留这一节，以免后来的读者把"缺失"误认为"完整"：没有步骤表，没有逐步的检查结果，也没有
     STOP 线小节，因为当时并没有被观察到的步骤。这个 run 花了多少成本、跑了多久、是否达成任何
     done-criterion，除非上面某一行写明，否则均为未知。 -->

无步骤表：该 run 是被接入的，不是被执行的。见 `metds/adopt.md` §5。
