# `/star-auto` — drive the workflow toward a stated goal

Read `.agents/commands/star-auto.md` and follow it with whatever the user typed beside `/star-auto` in this message.

For an unmarked skill, read the Cursor-owned copy under `.cursor/skills/` in full and follow it. For a skill marked † in the roster, dispatch a subagent that reads that Cursor-owned copy in full and follows it, as the shared file says.
