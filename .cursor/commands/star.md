# `/star` — route a request to the right STAR skill

Read `.agents/commands/star.md` and apply its router to whatever the user typed beside `/star` in this message.

For an unmarked skill, read the Cursor-owned copy under `.cursor/skills/` in full and follow it. No text beside the command selects `star-flow-status` with no argument.
