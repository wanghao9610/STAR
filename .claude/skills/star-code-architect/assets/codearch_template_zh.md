---
code_name: <CODE_NAME>
upstream: <仓库 URL，或 none (in-house)>
upstream_commit: <sha，或 —>
language: zh
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
model_id: <模型 id，照抄运行时本会话为你声明的那串——Claude Code 在会话开始注入；仅当本会话未声明任何模型才写 "unrecorded">
model_trail:                    # 只追加：每次写入会话一条，绝不改写既有条目
  - { date: <YYYY-MM-DD>, model: <模型 id 或 "unrecorded">, skill: <star-…>, scope: <本次会话写了什么> }
---

# 代码架构 — <CODE_NAME>

> 本项目代码组织方式的权威规范。智能体与人**写代码之前**先读这份文件。其他位置的摘要（AGENTS.md、编辑器规则）都指向这里；有冲突以本文件为准。

## 1. 目录布局

<!-- ${CODE_NAME}/ 的带注释目录树——每个目录一行职责。现状即基线；规划中的目录标注 (planned)。 -->

```text
<code_name>/
├── <dir>/        # <职责>
└── <dir>/        # <职责>
```

## 2. 放置规则

<!-- 各类新代码放哪。至少覆盖：模型组件、数据集/数据管线代码、配置、训练/评估逻辑、公共工具、脚本、测试。一行一条："新的 <某物> 放在 <路径>，通过 <机制> 注册。" -->

- 新的模型组件放在 `<路径>` ……
- 新的数据集放在 `<路径>` ……
- 新的配置放在 `<路径>` ……
- 实验产物一律不放这里——输出进 `wkdrs/<run>/`；数据进 `datas/`；权重进 `inits/`。

## 3. 命名与风格约定

<!-- 模块/文件命名、配置命名、类名前缀约定，以及风格基线（通常是：贴合上游风格）。勘察发现但未立迁移项的风格备注也记在这里。 -->

## 4. 计划组件落位映射

<!-- 研究计划（§3）的每个方法组件 → 现在或将来的位置。 -->

| 计划组件 | 目标路径 | 状态 |
|---|---|---|
| <组件> | `<路径>` | exists / planned |

## 5. 上游与出处

<!-- 一段话摘要：选定的仓库、原因（评分一句话）、许可证及其影响。完整元数据在 ${CODE_NAME}/UPSTREAM.md。 -->

## 6. 迁移记录

<!-- star-code-architect 执行（或受阻）的结构迁移，只增不删。 -->

| 日期 | 条目 | 从 → 到 | 状态 | 验证方式 |
|---|---|---|---|---|
| <YYYY-MM-DD> | M1 | `<旧>` → `<新>` | done / blocked | <检查> |

## 7. 改名残留

<!-- 有意保留的上游名称（注册表字符串、配置 type 键、与 checkpoint 耦合的名称、服务名）。每行：位置、类别、改名风险、建议的后续处理。 -->

| 位置模式 | 类别 | 风险 | 后续处理 |
|---|---|---|---|
| `<模式>` | <注册表字符串 / 配置键 / checkpoint / 服务名> | <一句话> | <保留 / 经 executor 步骤改名> |
