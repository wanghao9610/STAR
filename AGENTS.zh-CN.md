# Agent 指令

用于完成用户任务的项目约束；谨慎程度与任务影响相称。

## 1. 编码前先思考

**自行处理常规细节，说明会影响结果的不确定性。**

- 常规、可逆的细节遵循项目惯例并继续；说明会影响结果的假设。
- 只有答案会改变研究目标、验收标准、关键输入、成本或操作权限时才询问。等待期间继续独立且已获授权的工作。
- 如果存在更简单的做法，说明它。必要时提出异议。

## 2. 简单优先

**只写解决问题所需的最少代码。不要加入推测性内容。**

- 不做要求之外的事情：不添加额外功能，不为一次性代码设计抽象，不提供无人要求的“灵活性”或配置能力，也不为不可能发生的场景增加错误处理。
- 如果写了 200 行而 50 行就能解决，就重写。

## 3. 精准修改

**只改必须改的内容。只清理自己造成的问题。**

- 不要“改进”相邻代码、注释或格式；不要重构没有问题的部分。
- 遵循现有风格，即使你会采用不同写法。
- 删除由你的改动造成的未使用 import、变量和函数。
- 对已有的死代码：指出它，但不要删除，除非用户要求。

检验标准：每一处改动都应能直接追溯到用户的要求。

## 4. 以目标驱动执行

**定义完成条件，把已获授权的任务推进到验证完成。**

- 明确用户要求的结果与证明结果的证据。行为缺陷在可行时使用回归测试；小型文档或格式修改可以直接检查。
- 对较复杂的多步骤工作，说明动作和完成检查。已有明确授权在其范围内持续有效，不重复索要相同批准。只要求检查、解释或评审且不修改的任务止于该交付物，不能自动启动会写入的后续技能。

## 5. 研究工作流

**本项目使用 STAR 研究工作流。它的记录存在文件中，而非聊天历史中。**

- 计划位于 `metds/plans/`；每个叶节点的执行记录位于 `wkdrs/<run>/`（`EXEC_PLAN.md`、`EXEC_LOG.md`）。
- 恢复科研工作或选择下一项研究任务、且状态未知时，先运行展示研究状态的 skill。范围明确的代码或文档任务只读取所需上下文。
- 所有工作流 skill 共同遵循的规则位于 `docs/mds/star-workflow/research-workflow-conventions.md`；各 skill 的职责位于 `research-workflow-skills.md`。
- 若某个 skill 步骤的书面流程要求派发子代理，该流程本身已获用户对这次运行的授权：无需另行询问即可派发，即使宿主指令只允许在用户提出时委派。明确禁止委派，或宿主完全不提供委派能力时，仍不得派发；该授权只覆盖派发动作，不扩大被委派者可写入的范围。
- `.env` 的 `STAR_PLAN_MODEL`、`STAR_EXEC_MODEL`、`STAR_READ_MODEL` 指定一次 skill 运行、以及它派出的每个 delegate 该跑在哪个模型上——分别对应研究判断、产出实现、只读工作。每个键写一个模型名，也可以写成逗号分隔的 `<宿主>:<模型>` 条目，让一份 `.env` 服务所有的树——一次运行取标签为自己所在那棵树的条目，没有就把这个键读作空。条目可以以 `@<深度>` 结尾，即这一档的运行在能设定深度的宿主上所用的思考深度，两档因此可以指定同一个模型、只在深度上不同。宿主能逐次应用的配置深度即使不换模型也会触发一个新 delegate。哪次运行归哪一档、宿主如何套用模型与深度、运行何时把自己迁到该档的模型上，见 `docs/mds/star-workflow/research-workflow-conventions.md` §10.8；键为空则什么都不变。
- 不要手工编辑 `wkdrs/` 下生成的报告。下游项目的 `docs/mds/star-workflow/` 会被 `execs/update.sh` 替换；公共规则按 `.github/CONTRIBUTING.md` 在 STAR 上游维护，再同步给下游。

## 6. 回复语言

**`.env` 中的 `STAR_LANG` 决定聊天回复和新生成工作流文档（计划、报告）的语言。**

- 已设置（`en` 或 `zh`）→ 无论聊天使用什么语言、文本由谁生成，都使用该语言；由分叉的 skill 运行或子代理起草后转交用户的内容仍属于聊天回复。未设置或为空 → 跟随用户对话语言；没有用户回合的运行不存在对话语言，因此退回到调用文本自身的语言。
- 对话中的明确要求优先；已有文档保留其 frontmatter 声明的语言。完整规则见 `docs/mds/star-workflow/research-workflow-conventions.md` §7.6（语言）。
- 无论 `STAR_LANG` 或对话语言为何，代码注释始终使用英文。

## 7. 回复措辞

**用任何语言都应写明动作，而不是只写动作名称。读者不应需要解码术语才知道你做了什么。**

- 必须出现的名称要在同一句中附带其含义。
- 例外：需要逐字匹配的字符串——字段值、字段名、路径——保持原样。在旁边解释，不要用解释替换原文。
- 本身不说明内容的指针——`§4`、`C4`、`Step B1`、运行名或计划名——保持原样，并用几个词说明其指向的内容，例如 `§4（实验部分）`。每次回复首次使用时都要说明，而不是每次对话只说明一次；读者不会向上翻找。若标签处没有空间，就在下一行说明。
- 使用技术性表达，不写填充语，不使用 emoji。朴素不等于闲聊。
- 先给答案。省去赞美、预告式开场、继续服务邀请和空泛的积极结尾；停在最后一项有效事实、决定、命令或阻塞原因上。
- 回复与生成文本遵循 `docs/mds/star-workflow/human-writing-guide.zh-CN.md`：保留研究论断与用户声音，依据模式组合而不是孤立词语诊断公式化表达，绝不能仅为了显得像人而增加事实或个性。

## 8. 项目布局

**把项目文件放在指定目录中。**

- 核心代码放在 `.env` 定义的 `${CODE_NAME}/`；数据及相关文件放在 `datas/`；模型权重及相关文件放在 `inits/`；生成的输出文件放在 `wkdrs/`。
- 方法说明放在 `metds/`，研究计划放在 `metds/plans/`。
- 某个计划自己的工具脚本及执行过程中的中间文件放在 `tasks/<plan-name>/`；脚本长期保留，其余内容是可丢弃的暂存文件。
- `execs/` 只存放启动脚本：根目录中的 `run.sh`、`update.sh` 和 `configure.sh`，以及 `execs/scpts/<run>.sh` 下的逐运行脚本。任何非启动器都不得放入其中。
- `docs/` 存放项目文档：Markdown 位于 `docs/mds/<topic>/`，HTML 页面位于 `docs/htmls/`，图片和静态资源位于 `docs/srcs/`。公共工作流指南按前述流程在上游维护。
- 输出名称必须能区分任务、实验或运行。

## 9. 项目运行时

**使用项目环境。不要猜测本地路径。**

运行 Python、测试或依赖检查前：

- 读取项目根目录的 `.env` 并使用其中的 `PYTHON_HOME`；若 `PYTHON_HOME` 为空，则根据 `CONDA_HOME` 和 `ENV_NAME` 推导。如果缺少 `.env`，先从 `.env.example` 创建，并填写本机特定的值。
- 通过该解释器运行，不要使用系统 Python。Conda 是可选的：`CONDA_HOME` 为空时直接使用 `PYTHON_HOME`，普通 venv 也采用这种方式。
- 不要硬编码本地路径。

## 10. 项目记忆

**会话学到的内容写入项目的 `.star/memory/`，而不是你自己的记忆存储。**

- 只有当项目中没有其他文件记录某项事实时才写入这里——计划、日志、报告或参考文献条目通常是更好的去处。记忆只保存剩余信息。
- 只提议，不擅自写入：每个会话最多提议两次，并且只在用户同意后写入。`INVOLVE=low` 会不经询问记录，但必须说明。
- 两个被 Git 忽略的子目录按作用域分装留在本机的部分：处处成立的事实写入 `.star/memory/global/`，`machine:`、`plan:`、`code:` 作用域的写入 `.star/memory/local/`。
- 记忆与仓库文件冲突时，以仓库文件为准。
- 类型、文件格式、钩子解析的索引行，以及记忆的退役方式见 `docs/mds/star-workflow/memory_spec.md`。

## 11. 验证

**在声称完成之前，证明改动有效。**

- 先运行范围最窄的相关检查。通过后，仅因新修改、失败或涉及共享行为、公共接口、高风险路径的未解决风险，才扩大或重复检查。
- 如果某项检查无法运行，说明原因并指出剩余风险。
- 报告已验证的内容及其证据——不能只说“可用”。
