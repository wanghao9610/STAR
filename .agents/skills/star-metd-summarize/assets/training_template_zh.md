---
type: training
language: zh
generated: <YYYY-MM-DD>
model_id: <模型 id，写入时由运行时自报；运行时未提供则写 "unrecorded">
sources:
  - plan: <prefix>_<slug>_plan.md
    updated: <YYYY-MM-DD>
---

<!-- 由 star-metd-summarize 从 metds/plans/ 编译而成。手工编辑会在下次运行时被覆盖——要改这份
     文档，请改它的来源计划。 -->

# 训练

## 1. 训练流程总览

<!-- 端到端的各阶段（如 pretrain → align → finetune），顺序依叶子的 depends_on。一个阶段一行；细节在
     §2。单阶段训练就用一行说清，不要把它包装成流水线。 -->

| 阶段 | 数据 | 初始化自 | 训练什么 | 产出 |
|---|---|---|---|---|
| <1. 名称> | <数据集> | `inits/<…>` | <哪些参数> | `wkdrs/<run>/<…>` |

## 2. 逐阶段详情

<!-- 一个阶段一小节，来自训练类叶子的 §3 步骤与 §2 的 inits/ 输入，算力取根 §4 的预算。 -->

### 2.1 <阶段>

**优化目标。** <损失，以及它们把模型推向什么>
**可训练 / 冻结。** <哪些参数动，哪些不动>
**优化器与 schedule。** <优化器、LR schedule、warmup、epoch 或 step 数>
**算力预算。** <GPU 数 × 小时，按计划写明的>

## 3. 关键超参数

<!-- 复现者需要的东西。只写计划写明的值；没有哪个计划定死的值保持 `TBD` 并作为缺口汇报——在这里填一个
     看起来合理的默认值，就是把一个错数字写进论文。 -->

| 参数 | 阶段 | 取值 | 来源 |
|---|---|---|---|
| learning rate | <阶段> | <值> | `<计划> §<n>` |

## 4. 训练要点

<!-- 计划记录过的、让训练真正跑起来的东西：稳定性措施、已知失败模式及其早期信号、显存或吞吐措施、
     checkpoint 策略。来自训练类叶子的 §6 局部风险。没有就写"未记录"，不要发明建议。 -->

## 5. 复现方式

<!-- 逐阶段：入口、config，以及经项目 conda 环境的启动命令（计划用了 execs/run.sh 的就照写），加上输出
     落在 wkdrs/ 的哪里。命令按计划记录的原样写——这份文档是拿来读的，不是拿来执行的。 -->
