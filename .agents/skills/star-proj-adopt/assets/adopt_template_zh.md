---
code_name: <CODE_NAME>
language: zh
adopted: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
backfilled: <YYYY-MM-DD，或 — >
model_id: <模型 id，写入时由运行时自报；运行时未提供则写 "unrecorded">
---

# Project Adoption Record — 项目接入记录

> STAR 被加进这个仓库时，它是什么样子，以及接入过程接通了什么。本文件只作描述：记录存在什么，不记录
> 它们是为了什么。研究策略在 `metds/plans/`，代码架构在 `metds/codearc.md`，结果判断在 `wkdrs/` 下的
> 各份分析里。

## 1. 接入时的仓库

<!-- 首次提交日期、提交总数、主要语言与框架，以及用一段诚实的话说明这个项目当时已经在做什么。README
     里已经写明的地方直接引用，不要转述。 -->

## 2. 映射

<!-- 已确认的勘察结果，每条探测线一行。`置信度` 记的是门 1 当时的值，保留它是为了让后来的读者知道哪些
     行是探测出来的、哪些行是猜出来再被确认的。 -->

| 探测线 | 映射为 | 目标 | 置信度 | 备注 |
|---|---|---|---|---|
| 源码 | `CODE_NAME=<dir>` | — | | |
| 运行时 | `PYTHON_HOME=<path>` | — | | |
| 数据 | `datas/` | `<绝对路径>` | | 软链 / 本就在位 |
| 权重 | `inits/` | `<绝对路径>` | | |
| 输出 | `wkdrs/` | `<绝对路径>` | | |

## 3. 接通了什么

<!-- 本 skill 做过的每一次写入，以及每一次因路径已存在而跳过的写入。跳过的那些行与做成的同等重要：
     它们是"什么被原样留下"的记录。 -->

| 动作 | 路径 | 结果 |
|---|---|---|
| | | 已创建 / 已存在，原样留下 / 冲突，已询问 |

## 4. 工作清单

<!-- 每一行是一个可辨认的已完成或进行中的工作单元，见 references/adopt_spec_zh.md §5。
     只作描述。`state`：built / run / concluded / abandoned。每一行都带证据。 -->

| id | what | state | evidence | run_dir | metric（逐字引用） |
|---|---|---|---|---|---|
| W1 | | | | | |

## 5. 已入账的 run

<!-- 用户选择带进 wkdrs/ 的既往 run，以及有多少个仅作为证据留存。每个入账的 run 都有一份标注为重建的
     EXEC_LOG.md。 -->

| Run | 软链自 | 日期 | 重建日志 | 备注 |
|---|---|---|---|---|

仅作证据留存：<N> 个既往 run——见 §4。

## 6. 未决问题

<!-- 勘察定不下来、用户当时也还答不上来的事项。每条注明谁或什么能把它定下来。这一节为空是好结果；
     被凑满则不是。 -->

## 7. 回填记录

<!-- 由 `$star-proj-adopt backfill` 追加，每次运行一个带日期的块。每行：leaf、写入的字段、值，以及支持
     它的清单 id。用户否掉的 leaf 记为"已否决"——那是一个值得留存的决定，不是一处空白。 -->

### <YYYY-MM-DD>

| Leaf | 字段 | 值 | 依据 | 备注 |
|---|---|---|---|---|
