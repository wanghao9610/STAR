---
type: dataset
language: zh
generated: <YYYY-MM-DD>
model_id: <模型 id，照抄运行时本会话为你声明的那串——Kimi 会话有报告就照记；仅当本会话未声明任何模型才写 "unrecorded">
model_trail:                    # 只追加：每次写入会话一条，绝不改写既有条目
  - { date: <YYYY-MM-DD>, model: <模型 id 或 "unrecorded">, skill: <star-…>, scope: <本次会话写了什么> }
sources:
  - plan: <prefix>_<slug>_plan.md
    updated: <YYYY-MM-DD>
---

<!-- 由 star-metd-summarize 从 metds/plans/ 编译而成。手工编辑会在下次运行时被覆盖——要改这份
     文档，请改它的来源计划。 -->

# 数据集

## 1. 数据集清单

<!-- 方法涉及的每个数据集，一行一个，来自根 §4 的选择与每个叶子 §2 的 datas/ 输入。`路径`是计划点名的
     datas/ 下位置；`角色`写它被用来做什么，而不是它是什么。 -->

| 数据集 | 来源 | 使用的 split | 规模 | 路径（`datas/…`） | 角色 |
|---|---|---|---|---|---|
| <名称> | <出处 / license> | <train / val / test> | <样本数> | `datas/<…>` | <训练 / 评测 / 仅消融> |

## 2. 逐数据集详情

<!-- 只为"一行说不完"的数据集各写一小节：它贡献了什么、带什么标注、以及为什么它是这里的正确选择。
     一行就能说完的数据集直接跳过——把表格再抄一遍的小节是噪音。 -->

### 2.1 <数据集>

**为什么用它。** <它承担的、别的数据集替代不了的角色>
**它提供什么。** <图像 / 图文对 / 标签 / 标注>
**注意事项。** <域限制、偏置、license 约束——只写计划记录过的>

## 3. 预处理

<!-- 从原始数据到可训练数据，按顺序：缩放、tokenize、过滤、增强、格式转换。来自数据类叶子的 §3 步骤
     与它们产出的 §4 产物。计划点名了 ${CODE_NAME}/ 下模块的就写上；没有归属的步骤是一个缺口。 -->

## 4. 自建数据

<!-- 仅当方法自己造数据时才写：逐阶段的生成流水线、输入、决定留下什么的质量控制，以及产物落在 datas/
     的哪里。什么都不造时写"无——全部数据集按发布原样使用"。 -->

## 5. 统计信息

<!-- 读者判断规模与平衡所需的数字：各 split 样本数、类别分布、分辨率或长度范围。只写计划写明的数字。
     需要跑一次才能得到的数字在这里是缺口，而不是待办——它属于测它的那个 run 及其 wkdrs/ 下的分析
     报告。 -->

## 6. 数据集 → 实验映射

<!-- 哪个数据集服务哪条 claim 或哪个阶段，好让 training 与 evaluation 的协议有数据落点。来自根 §4 的
     claim→实验表与数据类叶子的 traces_to。 -->

| 数据集 | 用于 | 引用于 |
|---|---|---|
| <名称> | <训练阶段 / benchmark / 消融> | [training](training.md) §<n> |
