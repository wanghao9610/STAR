---
type: model_ledger
language: zh
generated: <YYYY-MM-DD>
model_id: <模型 id，照抄运行时本会话为你声明的那串——Kimi 会话有报告就照记；仅当本会话未声明任何模型才写 "unrecorded">
model_trail:                    # 只追加：每次写入会话一条，绝不改写既有条目
  - { date: <YYYY-MM-DD>, model: <模型 id 或 "unrecorded">, skill: <star-…>, scope: <本次会话写了什么> }
artifacts_scanned: <n>
artifacts_without_trail: <n>
---

# Model Ledger

<!-- 由 /skill:star-expt-digest ledger 写入。本文件是把磁盘上每份已注册产物的 model_trail 汇总而成的**生成物**，
     从不手工维护，在这里改它不产生任何效果。要修正某一行，去改它来源产物里的 trail，然后重新生成。
     每个值都抄自某条 trail，这里不做任何推断。trail 是自报的（规约 §8），本台账因此继承同一限制：
     它报告的是各运行时当时的声称，而非经核实的出处。 -->

## 1. 概要

<!-- 三到四句：扫描了多少份产物、其中多少带 trail、出现了多少个不同模型、覆盖的日期区间，以及有多少
     产物是单模型、多少是多模型。明确说明：某个模型写入事件少，并不能说明任何问题——这是一份出处记录，
     不是跑分。 -->

## 2. 写入事件

<!-- 跨全部产物的每一条 model_trail 条目一行，最新在前。这就是"流水"：同一份产物每被一次会话写入就
     出现一行，所以在模型之间交接过的文件会表现为连续几行、模型不同。尚无产物带 trail 则写"无"。 -->

| 日期 | 模型 | Skill | 产物 | 范围 |
|---|---|---|---|---|
| <YYYY-MM-DD> | `<model id>` | `star-plan-executor` | `wkdrs/<run>/EXEC_LOG.md` | steps 4–5, finalize |

## 3. 多模型产物

<!-- 只列 trail 中出现一个以上不同模型的产物——也就是发生过交接的那些。比较模型时值得读的正是这些行，
     因为同一份文件里同时承载了两个模型的工作。按写入先后顺序列出模型。若每份产物都只有一个作者，写"无"。 -->

| 产物 | 模型顺序 | 会话数 | 首次 → 最近 |
|---|---|---|---|
| `metds/plans/<prefix>_<slug>_plan.md` | `<model a>` → `<model b>` | 3 | <YYYY-MM-DD> → <YYYY-MM-DD> |

## 4. 各模型合计

<!-- 按产物种类统计各模型的写入事件数。只计数——绝不作质量判断，也绝不换算成比率（"X 写得多"不等于
     "X 做得好"）。种类即规约 §8 注册表的各行。 -->

| 模型 | 计划 | 运行 | 报告 | 编译文档 | 合计 |
|---|---|---|---|---|---|
| `<model id>` | 2 | 5 | 3 | 1 | 11 |

## 5. 缺口

<!-- 磁盘上不带 model_trail 的已注册产物（写于该字段存在之前，或某个 skill 漏写了），以及 model 为
     `unrecorded` 的 trail 条目。这些是本台账的盲区；读者必须能看清缺了多少，才好下结论。
     若每份产物都完整归属，写"无"。 -->

| 产物 | 缺口 | 为什么要紧 |
|---|---|---|
| `metds/codearc.md` | 无 `model_trail` | 早于该字段——它的作者身份是未知，而非单一模型 |
