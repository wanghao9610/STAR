---
type: framework
language: zh
generated: <YYYY-MM-DD>
model_id: <模型 id，照抄运行时本会话为你声明的那串——Kimi 会话有报告就照记；仅当本会话未声明任何模型才写 "unrecorded">
model_trail:                    # 只追加：每次写入会话一条，绝不改写既有条目
  - { date: <YYYY-MM-DD>, model: <模型 id 或 "unrecorded">, skill: <star-…>, scope: <本次会话写了什么> }
sources:
  - plan: <prefix>_<slug>_plan.md
    updated: <YYYY-MM-DD>
---

<!-- 由 star-metd-summarize 从 metds/plans/ 编译而成。手工编辑会在下次运行时被覆盖——要改这份
     文档，请改它的来源计划。 -->

# 框架

## 1. 总体架构

<!-- 根 §3 技术路线，写成一条数据通路：输入 → 依次经过的组件 → 输出。读者应该只看这一节就能把一个样本
     从头追到尾。示意图要小、以文本为主；逐组件的深度在 §2。 -->

## 2. 核心组件

<!-- 一个组件一小节，按 §1 的数据流顺序排，来自建模类叶子的 §1/§3 与其 §2/§4 点名的 ${CODE_NAME}/
     路径。只在计划给了公式时才写公式——为了显得严谨而发明的公式就是编造。 -->

### 2.1 <组件>

**职责。** <它是干什么的，一行>
**输入 → 输出。** <形状 / 模态>
**工作方式。** <计划描述的机制>
**关键公式。** <公式或伪代码——仅当某个计划给了时；否则删掉这一行>
**代码。** `${CODE_NAME}/<path>`

## 3. 关键设计决策

<!-- 那些本可以走另一条路的选择，以及计划给出的理由。这是 reviewer 第一个会问的。没有哪个计划记录过
     其理由的决策是一个值得 TODO 的缺口——如实说，而不是补一段听起来合理的说辞。 -->

| # | 决策 | 考虑过的替代方案 | 为什么选它 |
|---|---|---|---|
| D1 | <…> | <…> | <…> |

## 4. 与已有工作的差异

<!-- 根 §2 定位 + §3 novelty type，写给已经熟悉 baseline 的读者。差异要具体："我们加了一个模块"不是
     差异；这个模块让 baseline 做不到的什么成为可能，才是。 -->

## 5. 模块映射

<!-- 组件 ↔ 代码 ↔ 拥有它的计划，让文档始终能回溯到两边。没有代码路径的组件是还没建成的设计——该格
     留空，让本节的"尚未验证"标记来承载这个事实。 -->

| 组件 | 代码（`${CODE_NAME}/…`） | 归属计划 |
|---|---|---|
| <名称> | `<path>` | `<prefix>_<slug>_plan.md` |
