# Machine-specific settings. Copy and edit locally:
#   cp .env.example .env
#
# The runtime is selected by PYTHON_HOME. Either set it directly and leave
# CONDA_HOME / ENV_NAME empty, or leave it empty and let CONDA_HOME + ENV_NAME
# derive it as $CONDA_HOME/envs/$ENV_NAME. An explicit PYTHON_HOME always wins.
# Fill in CODE_NAME, then EITHER PYTHON_HOME alone, OR CONDA_HOME + ENV_NAME.
# Leave the keys of the route you are not using empty — a leftover placeholder
# is read as a real path and fails.
CODE_NAME=YOUR_CODE_NAME
ENV_NAME=
CONDA_HOME=
PYTHON_HOME=

# Optional. Upstream repo execs/update.sh pulls skills, hooks, and workflow
# guides from — point it at a fork to update from there instead. Resolution is
# environment first, then this file, then the default below.
STAR_REPOSITORY=https://github.com/wanghao9610/STAR.git

# Optional. Which agent harness trees execs/update.sh installs and keeps current,
# comma separated: claude, codex, cursor, dsh, kimi, pi, qwen — or all, which is
# the default, or none for the shared skeleton by itself. A tree left out is
# never written and never deleted, so whatever the project already has there
# stays. Resolution is environment first, then this file; --harnesses overrides both
# for one run.
STAR_HARNESSES=

# Optional. How much unresolved workflow judgment is asked about (conventions
# §7.7): low takes safe recommendations and records them; medium asks as needed;
# high asks for consequential choices individually. Existing explicit approval
# remains valid. Routine implementation and light validation stay within scope;
# new research decisions, unapproved cost and destructive operations still need
# specific authority. At low the executor presents its plan and proceeds without
# entering an approval mode. Read-only requests never authorize writing successors.
INVOLVE=medium

# Optional. Language the agents reply in and newly generated workflow
# documents (plans, reports) are written in (workflow conventions §7.6):
# en or zh. Unset or empty = follow the conversation's language; an explicit
# in-conversation request overrides it. Existing documents keep the language
# declared in their frontmatter — STAR_LANG never rewrites them.
STAR_LANG=

# Optional. Which model runs each tier of STAR work, where the harness can start
# a delegate on a named model. PLAN = research judgment: plans, plan reviews,
# analyses, blind reads. EXEC = implementation and production. READ = read-only
# scans, collectors, summaries. Empty = no change: the run stays on the model the
# session already has.
#
# A value is one model name, or comma-separated <harness>:<model> entries so one
# .env serves every tree — tags are STAR_HARNESSES' own (claude, codex, cursor,
# dsh, kimi, pi, qwen), the first colon ends the tag, and each name is spelled
# the way its own harness spells one. A run takes its tree's entry, falls back to
# an untagged one, and reads the key as empty when it has neither.
#
# An entry may end in @<depth> — low, medium, high, xhigh, max, or a positive
# integer — the thinking depth that tier reasons at where the harness can set
# one, so two tiers may name one model and differ only in depth; a configured
# depth sends the run to a delegate even when the model is unchanged. Cursor's
# stamp rewrites that suffix as [effort=<depth>] on the named agent's model
# field. An unavailable model or depth keeps the working route and is reported,
# never silently claimed.
#
# Run `bash execs/update.sh --models` after editing these keys, then start a new
# session. Full rule: workflow conventions §10.8.
#
# Example: STAR_PLAN_MODEL=fable, STAR_EXEC_MODEL=opus, STAR_READ_MODEL=sonnet
# — or, per tree and with depths:
# STAR_PLAN_MODEL=claude:fable@xhigh,codex:gpt-6-astra@xhigh,cursor:grok-4.6@xhigh,pi:anthropic/claude-fable-5-1
# STAR_EXEC_MODEL=claude:opus@high,codex:gpt-5.6-sol@high,cursor:grok-4.6@high,pi:anthropic/claude-opus-5
# STAR_READ_MODEL=claude:sonnet@medium,codex:gpt-5.6-terra@medium,cursor:grok-4.6@medium,pi:anthropic/claude-sonnet-5
STAR_PLAN_MODEL=
STAR_EXEC_MODEL=
STAR_READ_MODEL=
